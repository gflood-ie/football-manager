rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Production Rule: Only authenticated users can access storage.
      // TODO: Refine this to user-specific or team-specific paths as the app grows.
      allow read, write: if request.auth != null;
    }
  }
}
